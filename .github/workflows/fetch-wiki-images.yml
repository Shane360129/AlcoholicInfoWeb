name: 更新 Wikipedia 圖片快取

on:
  workflow_dispatch:

permissions:
  contents: write

# 強制 JS actions 以 Node 24 執行，消除 Node 20 淘汰警告
# （2026-06-02 起 Node 24 將成預設，屆時本設定為無作用、可移除）
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch Wikipedia 品牌縮圖
        run: node scripts/fetch-wiki-images.js

      - name: Fetch Wikimedia Commons 產品圖
        run: node scripts/fetch-product-images.js

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add images/wiki images/products data/wiki-images.js data/product-images.js
          if git diff --cached --quiet; then
            echo "圖片無變更，跳過 commit"
          else
            git commit -m "自動更新 Wikipedia / Commons 圖片快取"
            git push
          fi
